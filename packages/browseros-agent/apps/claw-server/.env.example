# Startup ports, CDP port, and resources come from the sidecar JSON passed
# with --config. Copy config.sample.json only when running the server manually.

# Optional BrowserClaw state root override.
# BROWSERCLAW_DIR="~/.browserclaw-dev"
